# macOS
.DS_Store

# Xcode — per-user state (not source)
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Build output
build/
DerivedData/
xcodebuild.log

# Local config (never commit). There is no model API key in Holmes Local;
# ~/.holmes holds mcp.json and the optional ElevenLabs key.
.holmes/
dist/
